import { Quote, ChevronLeft, ChevronRight } from 'lucide-react';
import { useState } from 'react';
import Reveal from './Reveal';
const items=[['OliTechs translated our ideas into a practical digital solution and stayed focused on what the business actually needed.','Business Partner','Hospitality Operations'],['The strongest part of the experience was the communication. Technical decisions were explained clearly and delivered with purpose.','Project Client','Business Owner'],['We needed technology that solved operational problems, not just something that looked good. OliTechs understood that distinction.','Technology Client','Operations & Systems']];
export default function Testimonials(){const[active,setActive]=useState(0);return <section id="testimonials" className="section section-alt"><div className="container"><Reveal><div className="section-heading"><span className="eyebrow">TESTIMONIALS</span><h2>What Clients Say</h2></div></Reveal><div className="testimonial-grid">{items.map(([q,name,role],i)=><Reveal key={name} delay={i*90}><article className={`testimonial ${i===active?'testimonial-active':''}`}><Quote className="quote-icon" size={22}/><p>“{q}”</p><div className="client"><span>{name.charAt(0)}</span><div><b>{name}</b><small>{role}</small></div></div></article></Reveal>)}</div><div className="testimonial-controls"><button aria-label="Previous testimonial" onClick={()=>setActive((active+2)%3)}><ChevronLeft size={17}/></button>{items.map((_,i)=><button key={i} className={i===active?'dot active':''} aria-label={`Show testimonial ${i+1}`} onClick={()=>setActive(i)}/>) }<button aria-label="Next testimonial" onClick={()=>setActive((active+1)%3)}><ChevronRight size={17}/></button></div></div></section>}
